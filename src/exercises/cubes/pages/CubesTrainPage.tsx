import { Link } from 'react-router-dom';
import { useSession } from '../../../app/SessionContext';
export function CubesTrainPage(){const {start}=useSession();const launch=(count:number)=>start({mode:'free',blocks:[{exercise:'cubes',level:'adaptive',itemCount:count}],briefing:[`Cubes 2D/3D — ${count} planches réelles.`,'Opposées, placements certains, anneau seulement si deux candidats restent.']});return <div className="max-w-3xl"><Link to="/cubes" className="text-sm text-sky-400">← Hub Cubes</Link><h2 className="mt-4 text-3xl font-bold">Entraînement réel</h2><p className="mt-2 text-zinc-400">Ces résultats alimentent tes statistiques réelles. Le cours guidé reste séparé.</p><div className="mt-6 grid gap-4 sm:grid-cols-2">{[5,10].map((count)=><button key={count} onClick={()=>launch(count)} className="rounded-2xl border border-zinc-700 bg-zinc-900 p-6 text-left hover:border-sky-500"><strong className="text-2xl">{count} planches</strong><span className="mt-2 block text-sm text-zinc-500">60 secondes maximum par planche</span></button>)}</div></div>}

